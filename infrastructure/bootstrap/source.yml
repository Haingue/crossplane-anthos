apiVersion: sourcerepo.gcp.upbound.io/v1beta1
kind: Repository
metadata:
  annotations:
    meta.upbound.io/example-id: sourcerepo/v1beta1/repository
    upjet.upbound.io/manual-intervention: Requires enabling `Cloud Source
      Repositories API` and granting `Source Repository Administrator` role
  labels:
    testing.upbound.io/example-name: anthos-config
  name: anthos-config
spec:
  forProvider: {}
  providerConfigRef:
    name: crossplane-anthos
